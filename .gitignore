/target
**/*.rs.bk
Cargo.lock.orig
.DS_Store
# web-ui build output (embedded at release)
crates/lemonfiber/web-ui/dist/
node_modules/
.planning/
.DS_Store
# coverage report, produced by `just coverage`
lcov.info
# agent tooling scratch — session state and throwaway worktrees, never the repo's
.claude/
