//! The `lemonfiber` binary — CLI, TUI and web UI over `lemonfiber-core`.
//! See spec `30-repos/cli.md`. Skeleton entry point.

fn main() {
    println!(
        "{} — see https://github.com/lemonfiber/spec (not yet implemented)",
        lemonfiber_core::PRODUCT
    );
}
