[package]
name = "lemonfiber-error"
description = "The one shape everything that goes wrong in lemonfiber takes, and how it is worded."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
license-file = "../../LICENSE"
publish = false

[features]
# `Code::new`, for a test that needs a code no problem carries. Taken only as a
# development dependency, so shipped code names every code through `codes`.
testing = []

[dependencies]
schemars = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
